# Cloudflare Configuration
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id
CLOUDFLARE_API_TOKEN=your_api_token
R2_BUCKET=clip-recap-pipeline

# Audio Processing Configuration
# MAX_CLIP_DURATION: Maximum clip duration in seconds (1-3600, default: 300)
MAX_CLIP_DURATION=300

# AUDIO_SAMPLE_RATE: Audio sample rate in Hz (8000-48000, default: 16000)
AUDIO_SAMPLE_RATE=16000

# AUDIO_CHANNELS: Number of audio channels (1-2, default: 1)
AUDIO_CHANNELS=1

# Development Configuration
# PYTHON_ENV: Python environment (development, production, test)
PYTHON_ENV=development

# DEBUG: Enable debug mode (true, false, 1, 0, yes, no, on, off, enabled, disabled)
DEBUG=false

# ALLOW_INSECURE: Set to 'true', '1', 'yes', 'on', or 'enabled' to disable SSL verification in development/testing
# WARNING: This should NEVER be enabled in production environments
ALLOW_INSECURE=false

# Security Configuration
# HMAC_SHARED_SECRET: Shared secret for HMAC authentication between Workers and Python API
# Generate with: openssl rand -hex 32
HMAC_SHARED_SECRET=your_secure_hmac_secret_here

# WORKERS_ORIGIN: Allowed origin for CORS (Cloudflare Workers domain)
# Use wildcard for all workers.dev domains: *.workers.dev
# Or specific domain: https://clip-recap-pipeline.paulchrisluke.workers.dev
WORKERS_ORIGIN=https://clip-recap-pipeline.paulchrisluke.workers.dev

# Rate Limiting Configuration
# REDIS_URL: Redis connection URL for distributed rate limiting (REQUIRED for production)
# Format: redis://username:password@host:port/db or redis://host:port/db
# Examples:
#   - Local Redis: redis://localhost:6379/0
#   - Redis Cloud: redis://username:password@redis-12345.c123.us-east-1-1.ec2.cloud.redislabs.com:12345/0
#   - Upstash Redis: redis://username:password@us1-capable-rat-12345.upstash.io:12345/0
# NOTE: In development mode, if REDIS_URL is not set, the system will fall back to in-memory cache
# NOTE: In production mode, REDIS_URL is REQUIRED for proper multi-instance rate limiting
REDIS_URL=redis://localhost:6379/0

# Test Configuration
# API_BASE_URL: API endpoint for security tests
# Default: https://pcl-labs-rf20o0ykn-pcl-labs.vercel.app/api/audio_processor
API_BASE_URL=https://pcl-labs-rf20o0ykn-pcl-labs.vercel.app/api/audio_processor

# CI: Set to 'true' in CI environments to enforce real secrets
# CI=false
